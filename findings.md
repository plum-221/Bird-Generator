# 调研发现

## Cloudflare Pages 第二站点（2026-07-28）

- 用户给出的 `yqlr.page.dex` 不是 Cloudflare Pages 的标准项目域名格式；Pages 项目 `yqlr` 对应的可用二级域名为 `yqlr.pages.dev`。
- 当前 Cloudflare 账号具备 Pages 写权限，`yqlr` 项目此前未占用；采用 Direct Upload 将本地 `dist/` 作为第二托管渠道，不影响原 GitHub Pages。
- Vite 默认根路径构建可直接部署到 Cloudflare Pages；生产包共上传 7 个文件，正式入口为 `https://yqlr.pages.dev/`。
- 新站真实 Chrome 验收通过：桌面鼠标和 390×844 手机触摸均进入 `pet-head / sustain / love`，松手进入 `end`；页面标题、Canvas 与正式哈希资源均正常加载。
- 本机代理会干扰 Wrangler 的 Pages API 请求；仅在单条 Wrangler 命令进程内临时清空代理环境变量即可正常创建、查询和部署，不修改系统代理配置。

## 持续抚摸交互（2026-07-28）

- 现有手势跟踪器在首次越过距离门槛后就停止发事件，因此模型只能得到一次表情脉冲；持续感需要在同一手势内按移动距离与时间节流续期。
- 将持续抚摸拆为 `start / sustain / end`，维持阶段使用约 `0.55s` 短时表情，结束阶段缩至 `0.42s`，既能覆盖正常移动间隔，又能在停手后快速恢复。
- 持续阶段不进入 `recent` 逗弄计数，仅一次手势的开始参与计数，因此长时间来回撸不会自行升级为生气。
- 身体横向抚摸实测保持托举目标为 `0`；向上拖动仍由既有 `body-lift` 高优先级状态接管。
- GitHub Pages 运行 `30339138841` 成功；线上新资源 `index-kcG-9J39.js` 的桌面鼠标和 390×844 手机单指均实测进入 `pet-head / sustain / love`。

## 表情期间额头羽纹遮挡（2026-07-28）

- `visible=true` 只能证明节点参与渲染，不能证明它位于头壳前方；本问题属于几何遮挡而非显隐状态错误。
- 最强后仰表情下，验收相机射线会先命中 SDF 头壳，再到达羽纹中心，旧实现的最差遮挡深度约为 `0.020` 个模型单位。
- 沿各羽纹的头部曲面法线增加 `headRadius * 0.08` 净空后，惊讶、受惊和得意恢复可见，中性、困倦及大小头型未出现明显悬浮。
- 回归测试采用“相机到羽纹的距离不得大于相机到头壳首次命中的距离”，能拦截节点存在但肉眼看不到的情况。
- GitHub Pages 运行 `30337201838` 成功；线上新构建的羽纹净空比例为 `0.086`，最强受惊后仰表情实拍可见六根羽纹。

## 翅膀与身体参数联动审查（2026-07-27）

- 当前翅根、尾根和髋部均为固定坐标；`chubbiness` 与 `furFluff` 改变体宽后，外接部件不会同步贴合。
- `headSize` 只改变头部半径，头部中心固定，极值时会破坏头颈融合。
- `legLength` 已进入参数表，但 `addFoot()` 未使用它生成髋踝距离，滑杆缺乏真实几何效果。
- 脚部虽使用 Toon 材质和背面描边，但细胶囊几何使明暗块与轮廓难以被看见，需要重绘踝掌和渐细脚趾。
- 用户最终确认小型窄叶翼：默认面积约为现有翅膀的 55%，基准翅根位置不变；体型变化时翅根须随体表联动，翅面始终贴侧身向后下方收。
- 实拍确认折翼几何必须位于身体侧面的 Y-Z 平面；仅缩放原 X-Y 平面翅膀仍会在正视图中表现为横向短杆。
- 三视图第二轮把翅根由体内移至体表，并只加宽翅面、不增加长度；正视不挡胸、侧视可读成小叶片、俯视不跨背。
- 极小头测试发现面部会被颈胸遮挡；按头径增加面部表面偏移后，眼睛和喙在参数下限仍可见。
- 腿长现在通过整体身体抬升改变髋—踝距离，脚掌始终保持地面高度；尾根、翅根、髋部和碰撞体同步使用派生解剖尺寸。
- GitHub Pages 运行 `30277797409` 成功；线上缓存破坏参数下重新拍摄正视、侧视、俯视，结果与本地 V4 一致。

- 原项目为纯前端 `Three.js + Cannon-es + Vite` 应用，无后端。
- 原项目包含程序化 SDF 身体、花色、物理玩具、天气、收藏卡、GLB、三语和移动端布局。
- 授权为 `PolyForm Noncommercial License 1.0.0`；用户已确认仅个人展示、非商业发布。
- 本机 GitHub 账号为 `plum-221`，`gh` 已登录；最终可发布到 GitHub Pages。
- 用户选择保留公共底层、独立重建鸟模块的路线 B。

## 上游源码初审（2026-07-27）

- 已合并上游 `d10eae5`，共导入 106 个文件、约 1.96 万行文本；合并提交为 `234e036`。
- `src/main.js`（约 3033 行）和 `src/catBuilder.js`（约 2302 行）是主要耦合点；实施前应先抽离角色接口，避免直接全局替换 `cat`。
- 可直接复用：`sdf.js`、`rng.js`、`hatch.js`、`rug.js`、天气基础层、相机、响应式面板和多数导出框架。
- 需鸟类化重建：`catBuilder.js`、`coats.js`、`catMotionStateMachine.js`、骨骼锚点/蒙皮、猫互动区域、鱼玩具与鱼雨、分享卡猫爪装饰。
- Motion 数据来自猫科资产；可以复用状态机框架，但鸟类飞行、扇翅、栖木需要自有骨骼和程序化动作，不能直接套猫科 19 骨骼。
- 当前 `package.json` 只有 4 个脚本测试，缺少构建前统一测试命令和鸟类参数边界测试。

## 已遇错误

- PowerShell 下 `rg src/*.js` 不接受该 glob 写法；已改为 `rg -g '*.js' ... src`，后续不重复原命令。

## 迁移边界细化

- `buildCat()` 的外部契约不仅是网格，还通过 `userData` 暴露碰撞体、头脸锚点、骨骼锚点、眼睛动画、花纹更新和构建耗时；`buildBird()` 必须先兼容这套公共契约再逐步改名。
- `main.js` 同时承担参数、场景、交互、UI 和 Motion 调度。首个实现步骤应建立角色配置/接口，随后小步迁移，不能一次性重写主文件。
- 原静态姿势有 8 种；鸟类需要逐项语义映射。鸟窝/容器模块可保留几何生成，但名称与适配尺寸需改为鸟窝/栖息场景。
- 分享卡中仓库地址、猫爪图案、标题和文件名均为硬编码；部署工作流中的 `/Meow-Generator/` 也必须随新仓库名更新。
- 当前 Motion 测试直接导入猫科状态机、19 骨骼蒙皮、猫姿势和鱼雨，因此不能只改 UI 文案，测试需要同步拆成公共测试与鸟类测试。

## 上游基线验证

- `npm ci` 成功，安装 16 个包。
- `test:share`、`test:fish-pick`、`test:poke`、`test:motion` 全部通过；Motion 基线为 14 动作、19 骨骼、8232 个采样。
- `npm run build` 通过；主 JS 压缩后约 1.52 MB，Vite 提示 chunk 超过 500 kB。该警告不阻塞迁移，但发布前应评估按模块拆包。

## 第一版鸟体视觉检查

- 浏览器实机截图确认新程序化鸟体已替代猫体，默认轮廓具备白色、圆肚、黑豆眼、黄喙和双翼，整体与原版三渲二描边兼容。
- 当前仍混有猫主题界面文案、猫玩具和猫资源；这是阶段 4 的明确迁移项，不是渲染失败。
- 透明 `fur` 命中网格与鸟体外形工作正常，现有镜头、地毯、光照和物理场景未因替换构建器而中断。
- 首次启动开发服务时 `Start-Process npm.cmd` 超时且未监听；改为直接隐藏启动 `node node_modules/vite/bin/vite.js` 后成功，后续沿用该方式。

## 完整迁移检查

- 已完成默认白色虎皮鹦鹉、5 套羽色、8 种姿势、14 个程序化动作、参数边界保护和原版交互契约兼容。
- UI、三语词条、收藏卡、GLB 文件名、Codex 预览、品牌图和部署路径均已鸟类化；README 明确保留上游署名和非商业许可。
- 保留原版物理玩具并新增两个鸟铃；种子雨替代鱼雨主题。旧猫模块保留在源码历史中但不进入当前主流程，便于审计与回退。
- 新增统一 `npm test`，采用失败即停止的串行测试；鸟参数、鸟动作和 4 组上游回归测试均已通过。
- 最终桌面截图确认白色鹦鹉、羽色面板、动作按钮和鸟类品牌资源正常显示，无可见运行时破损。
- 手机端 390×844 视口验证通过；底部主操作区和鸟体均在可操作范围内。
- GitHub Actions 构建与部署任务 `30264660549` 成功；线上地址 `https://plum-221.github.io/Bird-Generator/` 已实际打开验证。
- GitHub Actions 提示部分官方 Action 的 Node.js 20 运行时被强制迁移到 Node.js 24；当前不影响发布，后续升级 Action 主版本时复核。

## V2 视觉差异

- 参考照片的核心识别点是直立梨形身体、额头到胸部的连续轮廓、贴身翅膀、明显长尾和细腿；V1 的主体横向过宽，翅膀像两侧球体。
- V1 尾羽位于身体后侧且长度不足，正面几乎不可见；需要把尾根抬高、主尾羽拉长至身体下方。
- V1 面部没有眼睛高光，喙是单个圆锥，且面部未作为头部子节点；头部动作时存在脸与头不同步的结构风险。
- 程序化路线仍可满足需求，关键是用梨形旋转剖面和水滴形翼替代简单球体堆叠，无需引入外部雕刻模型。

## V2 实施错误记录

| 错误 | 尝试次数 | 处理 |
|---|---:|---|
| 新增结构测试提示缺少 `upperBeak` | 1 | 这是预期红灯；下一步重建上下喙并满足全部 V2 部件断言 |
| PowerShell 下 `rg ... README*` 把通配符当作非法路径 | 1 | 后续使用 `rg -g 'README*'`，不重复该写法 |
| V2 结构测试把斜向长尾计入身体宽度，误判纵向轮廓失败 | 1 | 将断言改为只测头部与躯干联合边界；尾羽由独立比例断言负责 |

## V2 第一轮浏览器检查

- 纵向轮廓、细腿、贴身翼和高光眼已经出现，结构测试给出 `0.84 × 1.43` 的纵向剪影。
- 胸部装饰层带独立深色描边，视觉上像贴在身体前面的巨大围兜，破坏了羽毛连续感；下一轮必须去掉胸腹内部黑边。
- 默认身体收窄过头，头和腹部显得像上下两个椭圆；需要适度加宽肩腹并强化头背到身体的连接。
- 眼睛与喙仍偏小，动漫表情不足；应增大眼睛约 20%，放大高光和钩喙，但不加入人类眉毛。
- 第二轮移除胸腹内部描边并加宽梨形身体；品牌图已重新生成，形态明显更接近参考照片，绿幕去背后透明边缘完整。
- 最终模型以三分之四角度展示，头身联合边界为 `0.90 × 1.09`，包含 126 个命名节点；斜向长尾单独计算，避免破坏躯干纵向断言。
- 桌面与 390×844 手机截图确认新模型没有遮挡主操作区，翅膀、额纹、鼻蜡膜、高光眼和尾羽可见。
- GitHub Actions V2 部署任务 `30266619788` 成功，线上页面已截图确认加载新模型与新资源路径。

## V3 SDF 接口审查

- `src/sdf.js` 已提供椭球 `sphere()`、渐缩 `roundCone()`、`smin()` 和 `meshFromSDF()`；无需引入新建模依赖。
- `meshFromSDF()` 输出带梯度法线的 BufferGeometry，可直接复用原版 toon 材质和背面放大描边。
- SDF 主体应只承担连续头颈/胸腹/肩背；折翼、尾羽、腿脚保留独立节点以支持动作，但根部必须嵌入主体。
- `speechBubbles.js` 的主角键仍为 `cat`，中英日三套字典和天气语句均残留猫、吃鱼、拍脸等内容；需要整体替换为 `bird` 角色，而不只是改几句中文。

## V3 实现与视觉复核

- 连续鸟体由腹部、胸部、颈部、头部、冠羽区和左右肩部 SDF 融合生成，网格标记为 `bird-sdf-v3`。
- 翅膀根点位于左右肩背，尾根位于后腰中线，腿根位于腹部下方；结构测试会拒绝根点回到 V2 的错误区域。
- 第一张浏览器截图显示侧翼仍过窄且埋入体表；将翼片加宽、根点外移并把近侧翼前移后，静态和展翅轮廓均清楚。
- 展翅动作截图确认双翼围绕肩部根点展开；前二后二脚趾可见，尾羽从尾椎向后下方延伸。
- 390×844 手机端画面完整，底部主操作区未遮挡鸟体关键部位。
- `npm test` 八组测试全部通过；`npm run build` 通过。Vite 仍提示主包约 843 kB，属于既有性能提醒，不阻塞本次发布。
- GitHub Pages 运行 `30268650193` 成功；线上缓存破坏参数下实测加载 V3。Actions 仍提示旧版官方 Action 的 Node.js 20 运行时被迁移到 Node.js 24，不影响本次发布。

## V3 实施错误记录

| 错误 | 尝试次数 | 处理 |
|---|---:|---|
| V2 模型缺少 `modelVersion: sdf-v3` 和解剖挂点 | 1 | 预期红灯；以 SDF 连续鸟体和显式挂点完成实现 |
| 旧气泡字典没有 `bird` 主角且残留猫台词 | 1 | 预期红灯；整体替换三语角色字典并保留旧调用兼容映射 |
| 移动端截图时浏览器自动化守护进程返回 EOF | 1 | 关闭会话并运行离线诊断后重启，第二次截图成功 |

## V3 脸部简化

- 原腮部使用 `SphereGeometry`，即使纵深压缩，三分之四视角仍会产生鼓起轮廓和立体阴影。
- 使用 `CircleGeometry + MeshBasicMaterial` 可保留淡色腮部识别，同时避免体积和光照阴影。
- 左右四个 `ThroatDot` 是用户所指的主要装饰小点；删除后鼻孔和额纹仍足以维持面部层次。
- 新增结构断言：左右腮部必须为平面圆形，且场景中不得存在 `ThroatDot` 节点。
- 旧实现首次运行新断言时按预期失败，实际类型为 `SphereGeometry`；完成替换后测试通过。
- GitHub Pages 运行 `30269806145` 成功；线上缓存破坏参数下确认平面腮色与简洁脸部已生效。
- PowerShell 对 `gh run list` 的彩色 JSON 输出执行 `ConvertFrom-Json` 会失败；后续直接使用 `gh --jq` 取运行编号。


## V3 腮部彻底移除

- 平面 `CircleGeometry` 虽无真实厚度，但独立圆形色块仍会被视觉识别为鼓起的腮帮；仅压平几何不能满足用户目标。
- 最终方案是删除左右腮部节点及 `cheekFlat` 材质，不再渲染任何独立腮部组件。
- 结构测试改为断言 `leftCheek`、`rightCheek` 均不存在，并继续断言无 `ThroatDot`。
- 沙箱状态切换后内置 `apply_patch` 和普通命令一度无响应；经授权后使用严格匹配的局部替换完成修改，并通过差异检查。
- GitHub Pages 运行 `30274045314` 成功；线上版本已确认不再包含任何独立腮部组件。

## V3 肩部轮廓修复

- 用户标注的“腮帮”是头颈下方左右轮廓，不是脸上的腮色或喉斑。
- `createBodyPrimitives()` 中两个半径 `0.14` 的肩部 SDF 球把 `0.72–0.86` 高度带顶到 `0.829` 宽，超过下胸 `0.782`，形成对称鼓包。
- 删除肩球后由胸部和颈部 SDF 自然融合，翅膀根点仍保留在 `attachments.leftWingRoot/rightWingRoot`，不会影响动作枢轴。
- 新轮廓断言要求肩部带宽不超过下胸带宽的 `1.02` 倍，防止对称鼓包回归。
- GitHub Pages 运行 `30274812768` 成功；线上版本已确认用户标注的左右肩部鼓包消失。

## 面部曲面吸附

- 眼睛与额头羽纹原先使用固定深度，头部尺寸变化后无法保持与椭球曲率一致。
- 新锚点根据头部椭球方程计算表面位置和法线；外层锚点负责贴合，内层 `EyeGaze` 负责眨眼与视线，避免动画覆盖曲面朝向。
- 最小头型会被身体的平滑融合外壳遮住，需额外增加与头径差值成比例的表面净空；默认头和大头不增加该补偿。
- 默认、小头、大头的正面与侧前视图通过；小头侧前视图中的细线是正常眨眼帧，连续截图可确认睁眼状态。
- 八组测试、生产构建和差异检查通过；仅保留既有 Vite 大包提示。
- GitHub Pages 运行 `30279261842` 成功；线上默认头和最小头缓存破坏地址均确认曲面吸附生效。

## 宠物互动与表情接入

- 原版软体戳碰、抓取、托举和玩具拖拽仍在 `main.js`/`softPoke.js` 中，当前问题不是底层功能消失，而是区域仍沿用猫语义且缺少鸟类表情反馈。
- 现有拾取只射线检测 `fur` 命中网格；鸟喙和独立翅膀要成为明确互动区，需要把命中对象标签一并传入区域判断。
- 表情应在现有待机和追视之后叠加，不能直接改写眼睛外层曲面锚点；眼睛形态只操作内层 `EyeGaze`。
- 旧“后颈提起”不适合鹦鹉，改为身体中下部托举；Motion 模式禁用软体拖拽和托举。
- 害羞红晕仅作为短暂屏幕覆盖特效，不恢复任何腮部模型几何。
- 直接沿用“玩具优先”的拾取顺序会让藏在鸟体后方的玩具抢走身体点击；必须同时检测鸟体、玩具和窝，再按相机射线距离取最近命中。
- 桌面和手机实测确认头、胸、喙、翅膀、身体、身体托举与玩具靠近均可触发；Motion 模式保留表情符号和台词，但不叠加软体抓取。
- 十种状态分别驱动内层眼睛、头部俯仰/侧倾、双翼、身体起伏和蓬松度；强符号只在情绪峰值出现，害羞只用屏幕红晕线。
- GitHub Pages 运行 `30282563052` 成功；缓存破坏地址实测命中 `pet-head → love`，新互动提示、模型眯眼、爱心和专属台词均来自线上构建。

## 本轮错误记录

| 错误 | 尝试次数 | 处理 |
|---|---:|---|
| 误读不存在的 `src/pokePhysics.js` | 1 | 实际文件为 `src/softPoke.js`，后续使用 `rg --files src` 先定位 |
| 搜索不存在的 `src/styles.css` | 1 | 实际样式文件为 `src/style.css`，不再重复旧路径 |

## V5 户外无限散步

- 采用半径 56 的大型策划地图而非运行时无限生成：环线路径、溪流木桥、花田、树林和环形远景让玩家能长时间散步，同时保持移动端性能和可测试性。
- 户外玩家必须使用同配色的独立站立鸟模型；直接复用室内当前模型会把睡姿或窝一起带到户外。
- 移动输入只能归一化一次。控制器若先输出方向向量，移动模型再按 `up/down/left/right` 解读，会导致键盘看似按下但坐标不变；现改为控制器输出按键集合、模型统一换算。
- 半径边界会把越界位置夹回 56，并暴露 `boundaryAmount` 供自动化检查；极限坐标 70 已实测回落到 56。
- 手机控制由场景模式、粗指针和宽度三项共同决定；测试专用 `mobile-controls=1` 只用于视觉验收，不影响普通桌面端。
- `?e2e=1` / `?test-muted=1` 会让背景音乐从用户暂停状态启动，浏览器实测 `playing=false`、`currentIndex=-1`。
- 10 只 NPC 复用 5 个草稿模型原型，降低几何构建成本；中英日各有独立名字与三句台词。
- Windows 上 agent-browser 自动拉起 Chrome 失败时，可启动独立 headless Chrome 调试端口后用 `--cdp` 接入；本轮据此完成桌面、手机和对话实拍。
